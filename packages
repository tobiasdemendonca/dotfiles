# not automated install yet!

# apt
sudo apt install -y \
    stow \
    tree \
    make \
    lazygit \
    net-tools \
    eza \
    build-essential \  # For building zellij from source
    btop \ 
    zoxide


# snap
vscode --classic
terraform --classic
go --classic
juju --classic

# From source or website
resvg # used for yazi svg rendering, download the binary from https://github.com/linebender/resvg/releases/latest and mv it in /usr/local/bin
zellij # requires cargo
zen   # tarball from their website
kitty # install script from website and their own configure script below it
fish  # it's an apt package but you'll need to configure the apt repo, just look at the readme: https://github.com/fish-shell/fish-shell
